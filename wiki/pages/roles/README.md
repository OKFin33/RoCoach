# Role Pages

Doctrine pages for Roco-native role reasoning.

Core rule:

```text
role = species baseline + selected set + team context
```

Do not assign one permanent global role to a species.

Current reviewed pages:

- `role_taxonomy.md`
