# Roco Handoff Archive

This directory keeps historical handoffs that are useful for audit but no
longer define current product or release scope.

Current entrypoint:

- `../ROCO_CURRENT_CONTEXT_MAP_2026_05_16.md`

Archive rule:

- keep evidence, do not delete it;
- never treat archived handoffs as current release contract;
- if an archived handoff conflicts with the current context map, the context
  map wins.
